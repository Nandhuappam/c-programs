#include<stdio.h>

void main() {
    int n,r,sum;
scanf("%d",n);
    sum=0;
    while(n!=0)
    {
        r=n%10;
        sum=sum*10+r;
        n=n/10;
        printf("r=%d,sum=%d ,n=%d\n",r,sum,n);
    }
    //printf("sum=%d",sum);
}
